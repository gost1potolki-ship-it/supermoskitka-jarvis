{
  "name": "calc-to-web-pc",
  "version": "1.0.0",
  "private": true,
  "description": "Офлайн-калькулятор: расчёт и экспорт КП в .txt",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "vite": "^5.1.4",
    "vitest": "^4.1.10"
  },
  "dependencies": {
    "firebase": "^12.14.0"
  }
}
